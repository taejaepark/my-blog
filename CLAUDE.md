# my-blog

마크다운 파일을 정적 블로그 웹사이트로 변환하는 프로젝트.

## 기술 스택

- 순수 HTML, CSS, JavaScript만 사용. 프레임워크(React, Vue 등)나 번들러(Webpack, Vite 등) 사용 금지.
- 빌드 스텝이 필요하다면 Node.js 스크립트 정도는 허용하되, 최대한 단순하게 유지할 것.
- 외부 라이브러리는 꼭 필요한 경우(마크다운 파서 등)에만 최소한으로 도입.

## 디자인 원칙

- 깔끔하고 읽기 좋은 타이포그래피 우선 (본문 폭 제한, 여백, 줄 간격에 신경 쓸 것).
- 다크 모드 지원 필수 — `prefers-color-scheme` 기반 자동 전환 + 수동 토글 버튼 병행.
- 모바일 반응형 필수 — 좁은 화면에서도 읽기 편해야 함.
- 색상, 폰트 등은 CSS 변수(custom properties)로 관리해 라이트/다크 테마 전환을 쉽게 할 것.

## 작업 시 유의사항

- 새 기능이나 리팩토링을 요청받지 않은 범위까지 확장하지 말 것.
- 불필요한 추상화나 설정 옵션을 미리 만들지 말 것.
- 주석은 WHY가 비자명한 경우에만 최소한으로 작성.
